to use the examples directory, cd into the root directory of the project and run:

```
export PYTHONPATH="vertix:$PYTHONPATH"
```

then running the python script in there using normal commands will work
